import { createTheme } from '@mui/system';

const theme = createTheme({
	bgColor: {
		primary: '#e8e8e8',
		secondary: '#bdbdbd'
	},
	color: {
		primary: '#454545',
		secondary: '#666666'
	}
})

export default theme;